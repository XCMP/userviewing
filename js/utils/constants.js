const CONTAINER_ATTRIBUTE = 'data-bltgc';
const CONTAINER_SELECTOR = '['+CONTAINER_ATTRIBUTE+']';
const ITEM_ATTRIBUTE = 'data-bltgi';
const ITEM_SELECTOR = '['+ITEM_ATTRIBUTE+']';
const EVENT_VIEW = 'view;';

const _consts = {
  CONTAINER_ATTRIBUTE,
  CONTAINER_SELECTOR,
  ITEM_ATTRIBUTE,
  ITEM_SELECTOR,
  EVENT_VIEW,
};

export default function CONSTS() {
  return _consts;
}